# JuegoFeliz
  Aplicacion para practivar mecanografia

# Requeriminetos
  HTML5 + CSS + JS
  node.js y jsDoc para generar la documentacion

# Compilacion
  Para generar la documentacion de JavaScript
   

# Git
  git add . -> añadimos todos los archivos
  git commit -m 'comentario' -> se realiza un commit
  git push -> se inserta los archivos
  archivo.gitignore -> colocamos los directorios o archivos que gitt tiene que ignorar